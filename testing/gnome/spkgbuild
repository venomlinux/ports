# description	: Meta port for GNOME Desktop Environtment
# depends	: meson gnome-shell gnome-shell-extensions gnome-session gnome-bluetooth gnome-keyring gnome-control-center gnome-menus polkit-gnome nautilus gnome-terminal gnome-backgrounds gdm

name=gnome
version=3.34
release=1

build() {
	mkdir -p $PKG/usr
}
