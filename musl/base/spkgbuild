# description	: Meta port for base system
# depends	: linux-api-headers man-pages musl tzdata zlib file readline m4 bc binutils gmp
# depends	: mpfr libmpc gcc bzip2 pkgconf ncurses attr acl libcap sed shadow psmisc 
# depends	: iana-etc bison flex grep bash libtool gdbm gperf expat inetutils perl perl-xml-parser
# depends	: autoconf automake xz kmod gettext libuargp elfutils procps-ng e2fsprogs coreutils
# depends	: diffutils gawk findutils groff less gzip iproute2 kbd libpipeline make patch
# depends	: eudev util-linux man-db tar texinfo vim scratchpkg dhcpcd runit-rc sysklogd filesystem

name=base
version=20200407
release=1

build() {
	mkdir -pv $PKG/usr
}
