# description	: BSD-style init-script
# backup	: etc/rc.conf

name=rc-init
version=0.2
release=1
source=(https://github.com/emmett1/rc-init/archive/v$version/$name-$version.tar.gz)

build() {
	cd $name-$version
	DESTDIR=$PKG ./INSTALL.sh
}
