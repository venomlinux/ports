post_install() {
	if [ $(type -p mkinitramfs) ]; then
		(
		cd /boot
		mkinitramfs 4.18.9
		mv initrd-4.18.9.img initrd-venom.img
		)
	fi
}

post_upgrade() {
	rm -f /boot/initrd-venom.img
	post_install
}
