# description	: Media player with built-in codecs that can play virtually all video and audio formats
# homepage	: http://smplayer.sourceforge.net/
# maintainer	: Emmett1, emmett1.2miligrams at gmail.com
# depends	: mplayer qt5 hicolor-icon-theme

name=smplayer
version=18.4.0
release=1
source=(https://downloads.sourceforge.net/$name/$name-$version.tar.bz2
	disable-werror-patch.patch)
md5sum=(6eb9f38e9f880aa29202beec076b8e0b SKIP)

build() {
	cd $name-$version

	sed '/gzip -9/d' -i Makefile

	patch -Np1 -i ../disable-werror-patch.patch

	make PREFIX=/usr \
	     DOC_PATH="\\\"/usr/share/doc/smplayer\\\"" \
	     QMAKE_OPTS=DEFINES+=NO_DEBUG_ON_CONSOLE
	make DOC_PATH=/usr/share/doc/smplayer \
	     DESTDIR=$PKG PREFIX=/usr install
}
