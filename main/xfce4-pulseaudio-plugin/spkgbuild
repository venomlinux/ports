# description	: Pulseaudio plugin for the Xfce4 panel
# depends	: xfce4-panel libnotify libcanberra

name=xfce4-pulseaudio-plugin
version=0.4.5
release=1
source="http://archive.xfce.org/src/panel-plugins/$name/${version%.*}/$name-$version.tar.bz2"

build() {
	cd $name-$version
	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--libexecdir=/usr/lib \
		--localstatedir=/var \
		--disable-static \
		--disable-debug \
		--disable-nls
	make
	make DESTDIR=$PKG install

}
