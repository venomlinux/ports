# description: Meta port for base-minimal system
# depends: linux-api-headers glibc binutils coreutils eudev util-linux kmod elfutils flex
# depends: ncurses patch sed kbd tzdata file iana-etc grep bash dash which procps-ng 
# depends: diffutils gawk findutils less bzip2 gzip tar scratchpkg iproute2 sysklogd 
# depends: rc filesystem pciutils usbutils iputils vim less man-db man-pages

name=base-minimal
version=20220606
release=1

build() {
	mkdir -pv $PKG/usr
}
