# description	: X.org Type1 fonts
# homepage	: http://xorg.freedesktop.org/
# maintainer	: emmett1, emmett1.2miligrams at gmail.com
# depends	: encodings font-alias fontconfig

name=font-xfree86-type1
version=1.0.4
release=2
source=(http://ftp.x.org/pub/individual/font/font-xfree86-type1-$version.tar.bz2)

build() {
	cd $name-$version

  	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--localstatedir=/var \
		--with-fontdir=/usr/share/fonts/Type1 \
		--disable-static
	make
	make DESTDIR=$PKG install

	rm -fr $PKG/usr/share/fonts/Type1/fonts.*
}

md5sum=(3eeb3fb44690b477d510bbd8f86cf5aa)
