post_install() {
	msginfo
	msginfo "Enable and start acpid service:"
	msginfo "  ln -sv /etc/sv/acpid /var/service"
	msginfo
}

rm_sv() {
	rm -f /etc/sv/$1/supervise
	rm -f /var/service/$1
}

pre_remove() {
	rm_sv acpid
}
