# package install script

action=$1
newversion=$2
oldversion=$3

case $action in
	post-install)
		for i in /var/lib/scratchpkg/index/*; do
			for ii in $i/*;	do
				[ -d $ii ] && rm -fr $ii
			done
		done
		;;
esac
