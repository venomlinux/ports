# description	: Meta port for base system
# depends	: linux-api-headers man-pages glibc tzdata zlib file readline m4 bc binutils gmp
# depends	: mpfr libmpc gcc bzip2 pkgconf ncurses attr acl libcap sed shadow psmisc 
# depends	: iana-etc bison flex grep bash libtool gdbm gperf expat inetutils perl
# depends	: autoconf automake xz kmod gettext elfutils procps-ng e2fsprogs coreutils
# depends	: diffutils gawk findutils groff less gzip iproute2 kbd libpipeline make patch
# depends	: eudev util-linux man-db tar texinfo vim scratchpkg dhcpcd sysklogd rc filesystem

name=base
version=20200802
release=1

build() {
	mkdir -pv $PKG/usr
}
