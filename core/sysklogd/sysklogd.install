post_install() {
	msginfo
	msginfo "Enable and start syslogd service:"
	msginfo "  ln -sv /etc/sv/syslogd /var/service"
	msginfo
}

rm_sv() {
	rm -f /etc/sv/$1/supervise
	rm -f /var/service/$1
}

pre_remove() {
	rm_sv syslogd
}
