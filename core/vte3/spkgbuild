# description	: Termcap file implementation for terminal emulators
# depends	: gtk3 pcre2 gnutls gobject-introspection libxml2 vala

name=vte3
version=0.52.0
release=2
source=(http://ftp.gnome.org/pub/gnome/sources/vte/${version::4}/vte-$version.tar.xz)

build() {
	cd vte-$version

	./configure --prefix=/usr          \
	            --sysconfdir=/etc      \
	            --disable-static       \
	            --enable-introspection
	make
	make DESTDIR=$PKG install
}

md5sum=(95b0d12864f7374128da33998e756e75)
