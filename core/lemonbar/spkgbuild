# description	: lightweight bar
# depends	: libxcb

name=lemonbar
version=1.4
release=1
options=""
noextract=""
backup=""
source="https://github.com/LemonBoy/bar/archive/v$version/$name-$version.tar.gz"

build() {
	cd bar-$version

	make VERSION=1.4
	make VERSION=1.4 DESTDIR=$PKG install
}
