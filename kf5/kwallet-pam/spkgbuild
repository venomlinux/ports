# description	: KWallet PAM integration
# depends	: libgcrypt linux-pam
# makedepends	: extra-cmake-modules qt5

name=kwallet-pam
version=5.12.1
release=1
source=(http://download.kde.org/stable/plasma/$version/$name-$version.tar.xz)

build() {
	cd $name-$version

	mkdir build
	cd    build

	cmake -DCMAKE_INSTALL_PREFIX=/usr	\
		 -DCMAKE_BUILD_TYPE=Release	\
		 -DBUILD_TESTING=OFF		\
		 -Wno-dev ..
	make
	make DESTDIR=$PKG install
}

md5sum=(cfa6fa62948d37fc3fe9351084c0bb6a)
